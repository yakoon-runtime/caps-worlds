title: Refine Split
resolvable: true
navigable: false
contextual: false
host: /boot/python/runtime
entry:
  run: pack:y5n.caps.worlds.apps.refine.split:main
invocation:
  params:
  - key: angle
    required: true
    positional: true
  - key: offset
resources:
  ref: resource:y5n.caps.worlds.resources.loader:content
  man:
    default:
      path: refine/split/man.ydf
